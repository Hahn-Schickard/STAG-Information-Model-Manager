#include "ModelManager.hpp"

#include <iomanip>
#include <iostream>

using namespace std;

int main() {
  auto manager = Infromation_Model_Manager::ModelManager::getInstance();

  exit(EXIT_SUCCESS);
}